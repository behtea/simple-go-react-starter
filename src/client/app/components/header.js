import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class HeaderComponent extends Component {
    render() {
        return (
            <div>
                <h1>Simple React & Go!</h1>
            </div>
        );
    }
}

export default HeaderComponent;
